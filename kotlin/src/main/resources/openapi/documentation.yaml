openapi: "3.0.3"
info:
  title: "kor_openapi API"
  description: "kor_openapi API"
  version: "1.0.0"
servers:
- url: "https://kor_openapi"
paths:
  /customer:
    post:
      description: "openAPI(path=\"openapi\", swaggerFile = \"openapi/documentation.yaml\"\
        ) <br> get(\"/\") { <br> val n =  call.parameters[\"name\"] <br> print(n)\
        \ <br> if (call.request.queryParameters[\"price\"] == \"asc\") { <br> print(\"\
        asc\") <br> // Show products from the lowest price to the highest <br> } <br>\
        \ call.respondText(\"Hello $n\") <br> } <br> get(\"/qstr\") { <br> //val ct\
        \ = call.request.contentType() <br> val p1 : Int?  =  call.parameters[\"my_p1\"\
        ]?.toIntOrNull() <br> val p2 : Int? =  call.parameters[\"my_p2\"]?.toIntOrNull()\
        \ <br> call.respondText(\"Hello test $p1\") <br> } <br> post<Customer>(){"
      requestBody:
        content:
          'application/json':
            schema:
              $ref: "#/components/schemas/Customer"
        required: true
      responses:
        "400":
          description: "Bad Request"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: "Your response content"
            '*/*':
              schema:
                type: "string"
              examples:
                Example#1:
                  value: "General exception"
        "201":
          description: "Created"
          content:
            text/plain:
              schema:
                type: "string"
              examples:
                Example#1:
                  value: ""
components:
  schemas:
    Customer:
      type: "object"
      properties:
        name:
          type: "string"
        age:
          type: "integer"
          format: "int32"